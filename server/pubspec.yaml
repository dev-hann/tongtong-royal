name: tongtong_server
description: WebSocket relay server (rooms, invite codes) for TongTong Royal.
version: 0.1.0
publish_to: none
resolution: workspace

environment:
  sdk: ^3.13.0

dependencies:
  shelf: ^1.4.0
  shelf_web_socket: ^2.0.0
  tongtong_shared:
    path: ../shared
  web_socket_channel: ^3.0.0

dev_dependencies:
  test: ^1.25.0
  very_good_analysis: ^6.0.0
